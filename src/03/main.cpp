#include "test_03.cuh"


int main(int argc, char** argv)
{
    // simpleDeviceQuery();
    // simpleDivergence();
    // nestedHelloWorld();
    nestedReduce2();
    // reduceInteger(atoi(argv[1]));
    return 0;
}